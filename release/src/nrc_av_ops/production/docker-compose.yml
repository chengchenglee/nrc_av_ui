version: '3'
services:
  backend_db:
    image: postgres:14-alpine
    container_name: nrc_av_deploy_prod_backend_db
    environment:
      - POSTGRES_USER=${DATABASE_USERNAME}
      - POSTGRES_PASSWORD=${DATABASE_PASSWORD}
      - POSTGRES_DB=${DATABASE_NAME}
    networks:
      - backend-db_net
    ports:
      - ${DATABASE_EXTERNAL_PORT}:5432
    volumes:
      - postgres-data:/var/lib/postgresql/data
    restart: always

  backend_service:
    build: ../nrc_av_web_api
    container_name: nrc_av_deploy_prod_backend
    environment:
      - SERVER_PORT=3000
      - DATABASE_HOST=${DATABASE_HOST}
      - DATABASE_PORT=${DATABASE_PORT}
      - DATABASE_USERNAME=${DATABASE_USERNAME}
      - DATABASE_PASSWORD=${DATABASE_PASSWORD}
      - DATABASE_NAME=${DATABASE_NAME}
      - CLIENT_URL=${CLIENT_URL}
    depends_on:
      - backend_db
    networks:
      - backend-service_net
      - backend-db_net
    ports:
      - ${BACKEND_PORT}:3000
    restart: always

  frontend_service:
    build:
      context: ../nrc_av_web_ui
      args:
        - VITE_BASE_URL=${API_URL}
    container_name: nrc_av_deploy_prod_frontend
    ports:
      - ${FRONTEND_PORT}:80
    restart: always

  flyway_service:
    image: flyway/flyway
    container_name: nrc_av_deploy_prod_flyway
    command: -url=jdbc:postgresql://${DATABASE_HOST}/${DATABASE_NAME} -user=${DATABASE_USERNAME} -password=${DATABASE_PASSWORD} -baselineOnMigrate=true -connectRetries=60 migrate
    volumes:
      - '../nrc_av_web_api/sql:/flyway/sql'
    networks:
      - backend-db_net
    depends_on:
      - backend_db

networks:
  backend-db_net:
  backend-service_net:


volumes:
  postgres-data:
